# Generated by the protocol buffer compiler.  DO NOT EDIT!

name: "Beta release ${{ inputs.environment }}"

on:
    push:
        branches:
            - main
    
    workflow_dispatch:
        inputs:
          environment:
            description: "Environment"
            type: environment
            required: true
jobs:
    create_release:
        runs-on: ubuntu-latest
        strategy:
          matrix:
            component: [WORKERS]
            suffix: [ledgers-wallets-loaderv2,ledgers-transactions-loaderv2,ledgers-ledgers-loaderv2]
            include:
              - component: WEBSERVERS
                suffix: ledgers
              - component: SQSWORKERS
                suffix: ledgers
              - component: SQSWORKERS
                suffix: ledgerv2                
        steps:
          - name: Release 
            run: echo ${{ matrix.component }} ${{ matrix.suffix }} 
 
